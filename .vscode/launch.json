{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Debug Test",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "purpose": ["debug-test"],
            "console": "integratedTerminal",
            "env": {"PYTEST_ADDOPTS": "--timeout=0"},
        }
    ]
}
